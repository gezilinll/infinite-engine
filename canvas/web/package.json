{
  "name": "infinite-canvas",
  "version": "1.0.0",
  "description": "",
  "main": "./src/InfiniteEngine.ts",
  "scripts": {
    "build:ts": "npx tsc -p tsconfig.json",
    "build:lib": "rm -rf dist && npx vite build --config vite.config.lib.js && npx tsc -p tsconfig.json",
    "build:page": "rm -rf dist && npx vite build --config vite.config.page.js && tsc -p tsconfig.json && mv dist/assets dist/demo",
    "dev": "NODE_OPTIONS=--max_old_space_size=2048 vite"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.8",
    "leancloud-storage": "^4.9.0",
    "tslint": "^6.1.3",
    "typescript": "^4.8.4",
    "url-join": "^4.0.1",
    "vite-plugin-top-level-await": "^1.2.4"
  },
  "dependencies": {
    "vite": "^4.1.1"
  }
}